# Copy this file to `.env` and fill in your own keys.
# `python-dotenv` (loaded from main.py) will pick them up automatically.

# Required for any gpt-* model.
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# Required for any gemini-* model.
GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY"
